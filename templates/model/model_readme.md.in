# {{CLASS_NAME}}

> Status: scaffold generated by `scripts/new_model.py` on {{DATE}}.

- Task: `{{TASK}}`
- Config section: `[{{SECTION}}]`
- Output contract: `jinq::models::io_define::{{IO_NAMESPACE}}::{{OUTPUT_TYPE}}`
- Backend: `{{BACKEND}}`
- Model config: `conf/model/{{MODEL_DIR}}/{{NAME}}/{{NAME}}_config.toml`

## What is already done

- Header + implementation skeleton wired to `BackendCvModel<INPUT, OUTPUT>`.
- `[{{SECTION}}.backend]` / `[{{SECTION}}.params]` TOML skeleton.
- An output contract test that fails explicitly while the model is unimplemented.

## TODO(new_model)

- [ ] `preprocess`: request image -> named input tensors.
- [ ] `postprocess`: named output tensors -> `{{OUTPUT_TYPE}}`.
- [ ] `on_init`: read `[{{SECTION}}.params]`.
- [ ] Register the model in `{{CATALOG_HEADER}}`.
- [ ] Add a golden case with real weights.

## Verify

```bash
cmake --build <build-dir> --target {{FILE_BASE}}_output_contract_unittest
```
