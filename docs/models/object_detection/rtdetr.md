# RtdetrDetector

> Status: scaffold generated by `scripts/new_model.py` on 2026-08-30.

- Task: `object_detection`
- Config section: `[RTDETR]`
- Output contract: `jinq::models::io_define::object_detection::std_object_detection_output`
- Backend: `tensorrt`
- Model config: `conf/model/object_detection/rtdetr/rtdetr_config.toml`

## What is already done

- Header + implementation skeleton wired to `BackendCvModel<INPUT, OUTPUT>`.
- `[RTDETR.backend]` / `[RTDETR.params]` TOML skeleton.
- An output contract test that fails explicitly while the model is unimplemented.

## TODO(new_model)

- [ ] `preprocess`: request image -> named input tensors.
- [ ] `postprocess`: named output tensors -> `std_object_detection_output`.
- [ ] `on_init`: read `[RTDETR.params]`.
- [ ] Register the model in `factory/obj_detection_task.h`.
- [ ] Add a golden case with real weights.

## Verify

```bash
cmake --build <build-dir> --target rtdetr_detector_output_contract_unittest
```
