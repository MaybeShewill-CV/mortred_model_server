# YOLOV8 端到端链路耗时迭代记录（C=1 / raw 传输）

目标：http_infer_rps.py 实测 **C=1 raw P50 ≤ 13ms** 且 **C=16 raw RPS ≥ 120**（P50 用 C=1 测量，RPS 用饱和并发测量——C=1 闭环下 rps=1/延迟，两者无法在同一并发下同时满足）。
方法：每轮迭代 = 单项改造 → 重编译 → 重启 YOLOV8 → golden 数值对比 → C=1×3 轮(60s+5s warmup, raw) + C=16×1 轮(60s+5s, raw) 埋点实测 → 与历史对比落盘本文件。
环境：RTX 2070 SUPER / TensorRT 10.3 / WSL2 / 埋点分支 `perf/stage-timing`（`/tmp/mortred_stage_timing` 开关）。
输入：bus.jpg（810×1080 JPEG，487KB，5 个检测框，golden 基准 `/tmp/golden_pre_w1.json`）。

## 基线（改造前，2026-09-19，埋点 6531 样本）

| # | 环节 | raw P50 (ms) | raw mean (ms) |
|---|---|---|---|
| 1 | 客户端+socket①+网关框架壳（残差） | ~0.58 | ~0.82 |
| 2 | 网关处理（route+auth+fwd） | 0.077 | 0.089 |
| 3 | socket② 往返+网关回调拷贝（残差） | ~0.58 | ~0.61 |
| 4 | 模型服务框架与调度（envelope/admit/go/worker/reply/serialize） | 0.184 | 0.19 |
| 5 | decode（imdecode 810×1080） | 6.262 | 6.375 |
| 6 | pre（cvtColor+resize+pad+convert+scale+CHW 五步链） | 4.235 | 4.529 |
| 7 | TRT h2d（pageable memcpyAsync 4.9MB） | 1.312 | 5.796 |
| 8 | TRT exec（enqueueV3+GPU+sync） | 3.244 | 9.490 |
| 9 | TRT d2h | 0.802 | 1.090 |
| 10 | post（8400 候选扫描+NMS+坐标还原） | 0.999 | 1.099 |
|  | **客户端合计** | **19.03** | **30.01** |

客户端指标：C=1 P50 19.03ms / RPS 33.3；C=16 RPS 79.5（同窗口背靠背实测）。

---

## 迭代 1：W1 融合预处理核（letterbox_bgr_f32_nchw）

改动：`model_runtime.{h,cpp}` 新增 `letterbox_bgr_f32_nchw`（resize 后单遍平面写入，消除 cvtColor/全图 pad/convertTo/scale/CHW 双拷贝五个全图中间体，数值逐比特等价：resize 通道无关可与 BGR→RGB 交换次序，pad 与像素走同一 `(float)px*(1/255f)` 单乘）；`yolov8_detector.inl` preprocess 优先融合路径，旧五步链保留为回退。

**精度门禁**：bus.jpg golden 对比——5 框→5 框，全部字段差异 `0.000e+00`（逐比特等价，与设计一致）。

**实测**（C=1×3 取中位，n=7696 埋点样本；C=16×1）：

| # | 环节 | 基线 P50 | **W1 P50** | Δ | W1 mean | 备注 |
|---|---|---|---|---|---|---|
| 1-4 | 传输/网关/框架壳合计（残差） | ~1.32 | ~1.23 | −0.09 | — | 与模型 total−客户端差一致 |
| 5 | decode | 6.262 | 6.185 | −0.08 | 6.350 | 未动，噪声带内 ✓ |
| 6 | **pre** | **4.235** | **1.545** | **−2.69** | 1.831 | 融合核收益兑现 |
| 7 | h2d | 1.312 | 1.330 | +0.02 | 4.990 | 未动 ✓ |
| 8 | exec | 3.244 | 3.381 | +0.14 | 8.145 | 未动，GPU 噪声 ✓ |
| 9 | d2h | 0.802 | 0.840 | +0.04 | 1.393 | 未动 ✓ |
| 10 | post | 0.999 | 0.950 | −0.05 | 1.079 | 未动 ✓ |
|  | 模型 total | 17.87 | 15.201 | −2.67 | 23.998 | closure: 15.20+1.23≈16.43 ✓ |
|  | **客户端 P50 / RPS(C=1)** | **19.03 / 33.3** | **16.43 / 39.5** | **−2.60 / +19%** | mean 25.28 | 三轮 16.15/16.43/16.50 |
|  | **RPS(C=16)** | **79.5** | **102.9** | **+29%** | P50 141.95 | 饱和态 pre 13.24→3.46，排队 131.8→96.7 |

单变量归因验证：仅 pre 格显著变化 ✓；Σ校验通过 ✓。
距目标：P50 16.43（需 ≤13，还差 3.43）｜RPS 102.9（需 ≥120，还差 17）。

---

## 迭代 2：W2 pinned staging buffer（TrtSession H2D/D2H）

改动：`trt_session.{h,cpp}` 增加常驻 pinned staging（按输入/输出总字节切片布局，动态输出回退 pageable，分配失败静默回退，mutex 保险）。

**精度门禁**：golden 对比 5 框→5 框，`0.000e+00`（staging 不改变数据）。

**实测**（C=1×3 取中位，n=8136；C=16×1）：

| # | 环节 | W1 P50 | **W2 P50** | Δ | 备注 |
|---|---|---|---|---|---|
| 5 | decode | 6.185 | 6.203 | +0.02 | 未动 ✓ |
| 6 | pre | 1.545 | 1.546 | ~0 | 未动 ✓ |
| 7 | **h2d** | 1.330 | **0.751** | **−0.58** | pinned 生效（含新增 host memcpy ~0.2） |
| 8 | exec | 3.381 | 3.981 | +0.60* | *C=1 下 GPU 桌面争抢噪声带；见下行 C=16 才是真实效果 |
| 9 | d2h | 0.840 | 0.794 | −0.05 | |
| 10 | post | 0.950 | 0.941 | ~0 | 未动 ✓ |
|  | 模型 total | 15.201 | 14.950 | −0.25 | |
|  | **客户端 P50 / RPS(C=1)** | 16.43 / 39.5 | **16.21 / 42.4** | −0.22 / +7% | 三轮 16.21/16.42/16.01 |
|  | **RPS(C=16)** | 102.9 | **140.2** | **+36%** | P50 141.9→100.6 |

C=16 分解（P50）：worker 排队 96.7→67.8、h2d 2.32→1.34、exec 9.95→3.59（pageable 暂存曾拖住 stream，pinned 后 sync 等待坍缩）、pre 3.46→3.29，total 138.5→98.0。**C=16 RPS 140.2 ≥ 120：吞吐目标已达成**。
距目标：P50 16.21（需 ≤13，还差 3.21）｜RPS 140.2 ✓ 已达标。

---

## 迭代 3：W3 后处理候选扫描顺序化

改动：`yolov8_decode.h` 的 `collect_yolov8_candidates` 由按 anchor 跨步行读类分数（每读一步 33.6KB，全 cache miss）改为**按类行顺序扫描 + 每 anchor running max**（语义逐比特等价：同样的严格 > 升序类决胜、无副作用过滤器顺序不可观测），几何行一次拷贝后顺序处理。

**精度门禁**：golden 5 框→5 框，`0.000e+00`。

**实测**（C=1×3 取中位，n=8476；C=16×1）：

| # | 环节 | W2 P50 | **W3 P50** | Δ | 备注 |
|---|---|---|---|---|---|
| 5 | decode | 6.203 | 6.191 | ~0 | 未动 ✓ |
| 6 | pre | 1.546 | 1.534 | ~0 | 未动 ✓ |
| 7 | h2d | 0.751 | 0.726 | ~0 | 未动 ✓ |
| 8 | exec | 3.981 | 4.034 | +0.05 | GPU 噪声带 ✓ |
| 9 | d2h | 0.794 | 0.782 | ~0 | ✓ |
| 10 | **post** | 0.941 | **0.781** | **−0.16** | 候选扫描收益（NMS 部分未动） |
|  | 模型 total | 14.950 | 14.708 | −0.24 | |
|  | **客户端 P50 / RPS(C=1)** | 16.21 / 42.4 | **15.98 / 42.9** | −0.23 | 三轮 16.00/15.79/15.98 |
|  | **RPS(C=16)** | 140.2 | **150.3** | +7% | P50 100.6→97.2 |

距目标：P50 15.98（需 ≤13，还差 2.98）｜RPS 150.3 ✓ 已达标。剩余缺口全部指向 decode（6.19ms）→ 进入 W4。







